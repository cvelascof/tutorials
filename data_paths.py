## data paths (specify the relevant paths*)  
path_inputs  = "/home/username/pySTEPS/data/archive"   # the root path to the input radar files**
path_outputs = "/home/username/pySTEPS/data/out"   # the path where to save outputs (figures, forecasts, etc)
# (*) do not include the backslash / at the end of the path
# (**) this folder should contain two subdirectories (fmi and mch), each one with a list of radar files.
# It can be created by unzipping the file archive.zip in the directory /home/username/pySTEPS/data. 
# (archive.zip is available on https://pysteps.github.io/).
